.env
.env.*
!.env.example
node_modules/
.next/
dist/
coverage/
*.log
# TypeScript's incremental build cache.
*.tsbuildinfo
# Regenerated by Next.js on every dev/build. It imports from .next/, which is not
# in the repository, so committing it would leave a fresh clone with unresolved
# imports.
next-env.d.ts
