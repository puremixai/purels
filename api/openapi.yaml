openapi: 3.0.3
info:
  title: Purels API
  version: 0.1.0
servers:
  - url: http://localhost:8080
paths:
  /api/v1/auth/login:
    post:
      summary: Log in administrator
  /api/v1/links:
    get:
      summary: List links
    post:
      summary: Create link
  /{alias}:
    get:
      summary: Redirect short link
